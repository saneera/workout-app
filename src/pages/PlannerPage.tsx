import { NativeStackHeaderProps } from "@react-navigation/native-stack";
import { Text, View } from "react-native";

const PlannerPage = ({ navigation }: NativeStackHeaderProps) => {
  return (
    <View>
      <Text>Planner Page</Text>
    </View>
  );
};

export default PlannerPage;
